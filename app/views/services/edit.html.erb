<%= page_title "Edit service" %>

<div class="page_head">
  <h1>Edit Service</h1>
</div>

<div class="edit person">
  <%= form_for @service do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p class="button"><%= f.submit "Save changes", :disable_with => 'Saving changes' %> <%= link_to "Cancel", settings_path %></p>
  <% end %>
</div>
