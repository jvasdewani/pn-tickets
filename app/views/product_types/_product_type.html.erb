<%= div_for product_type, class: 'person' do %>
	<h2><%= product_type.name %></h2>

  <div class="controls">
    <p><%= link_to "Edit", edit_product_type_path(product_type) %> <%= link_to "Delete", product_type, :method => :delete, :class => 'notice', :confirm => "Are you sure you want to remove this product type?" %></p>
  </div>
<% end %>
